Generate remediation artifacts from step1_output.md and step2_output.md (appended below). After your skill’s **suite preflight**: **`Test#Method`** `proposed_fix_*.md` **only** when **any** Phase 0 suite artifact exists (**`invocation_summary.txt`** and/or **`test_result_failures_suite.html`** under **`test-results/`** or Jenkins mirrors — same gate as triage Phase 0); if **none** exist, use CVD-only / infra fixes — **not** suite fixes from XML alone. **Suite mode file count:** **one** `proposed_fix_*.md` **per** distinct failing **`Test#Method`** from Step 1 (Step 2 must not collapse them into one umbrella infra doc) — **unless** triage marked **`(xN identical)`** for that group. If Step 1’s failure table has **R ≥ 2** methods (no `(xN identical)` fold), you **must** emit **≥ R** files — **never** exactly **one** suite-wide fix (e.g. a single speculative `SystemServer` / framework patch) for the whole table. **`## [FAILURE_ID] Summary`** must use each row’s full **`Test#Method`**, not only `[INFRASTRUCTURE_FAILURE]`. Otherwise treat **`[GUEST_KERNEL_*]`** / **`[HOST_*]`** RCAs as authoritative for infra-style fixes. **Strict:** write **only** under **`gemini-assist/`** — filenames **`gemini-assist/proposed_fix_{failure_type}_{timestamp}.md`** (never workspace root, never **`test-results/`**, never bare **`proposed_fix.md`**). Put **all** narrative (including image-build workarounds) **in** those files, including mandatory **`## Human follow-up - image build review`**. Final reply: **only** a bullet list of paths (each must start with **`gemini-assist/`**) — **no** JSON, **no** `"response":` prose. Follow your skill instructions.
