Triage under `test-results/` (workspace-relative; not host path `/test-results`). **Decide mode (first line of reply: `MODE: CTS_SUITE` or `MODE: CVD_ONLY` only — see skill Phase 0):** **`MODE: CTS_SUITE`** if **any** of these exist: **`test-results/invocation_summary.txt`**, **`test-results/test_result_failures_suite.html`**, **`android-cts-results/invocation_summary.txt`**, **`android-cts-results-html/test_result_failures_suite.html`**. List failed tests from **`test_result_failures_suite.html`** when present; **otherwise** from **`invocation_summary.txt`** + **`test_result*.xml`** (batched per skill). Then **Phase 2** kernel preflight once per guest `kernel.log`, **Phase 2** logcat preflight (exception-first per skill), plus per-test greps (no full Phase 1b, no **`[GUEST_KERNEL_*]`** rows). **Before the Phase 4 table**, emit **`## Kernel preflight (machine-readable)`** and **`## Logcat preflight (machine-readable)`** with **`KERNEL_PREFLIGHT_HITS:`** / **`LOGCAT_PREFLIGHT_HITS:`** when those logs exist. **If none** of those four suite artifact paths exist — even if **`test_result*.xml`** has **`result="fail"`** — use **`MODE: CVD_ONLY`** (do **not** choose suite mode from XML alone): same Cuttlefish path as **CVD Launcher `triage-cvd`** — **userspace logcat** is usually **`logcat`** under **`**/logs/`** (not `kernel.log`). Unzip **`test-results/cuttlefish_logs*.zip`** into `test-results/cvd/` if guest **`kernel.log`** is missing; read guest **`test-results/cvd/**/logs/kernel.log`** before flattened `cvd-*.log`; Phase 1b-A / 1b, **`launcher.log`**, **Phase 1d** (exception-first per skill), wifi. **Before the Phase 4 table** when **`MODE: CVD_ONLY`**, emit the same **`## Kernel preflight`** / **`## Logcat preflight`** machine-readable sections per **`triage-cts`**. Emit **`[GUEST_KERNEL_*]`** / **`[LOGCAT_*]`** when greps match. Follow your skill instructions.
