Root cause analysis from step1_output.md (context may be appended below). **CTS suite path:** when **any** Phase 0 suite artifact exists (**`test-results/invocation_summary.txt`** and/or **`test-results/test_result_failures_suite.html`**, or mirrors under **`android-cts-results*`**), RCA **`Test#Method`** failures (HTML first if present; else summary + **`test_result*.xml`** for detail); use CVD/guest logs as supporting evidence per test. **CVD-only path:** when **none** of those files exist — even if failing XML exists — guest **`kernel.log`** / **`launcher.log`** before host `cvd-*.log`. Follow your skill instructions.
