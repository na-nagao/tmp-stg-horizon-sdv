Generate remediation artifacts for each distinct failure from Step 2 (host/CVD/guest/image/infra). Step 1 and Step 2 output are in step1_output.md and step2_output.md (context may be appended below). **Honour `CVD_STATUS`** from Phase 0: if Step 1 classified the run as **`[CVD_HEALTHY]`** (`BOOT_OK` with no actionable rows), write **one** file **`gemini-assist/proposed_fix_runtime_observations_<timestamp>.md`** summarising informational warnings and stating no remediation is required — or reply only `FIX_UNKNOWN: no action required.` if there were none. On `BOOT_OK` with runtime buckets (`[GUEST_RUNTIME]`, `[LOGCAT_*]`, `[NETWORK_WIFI]`), propose fixes for the **runtime** issue only; do not synthesize boot-failure narratives. **Strict:** write **only** under **`gemini-assist/`** — filenames **`gemini-assist/proposed_fix_{failure_type}_{timestamp}.md`** (never workspace root, never **`test-results/`**, never bare **`proposed_fix.md`**). Final reply: **only** a bullet list of paths (each must start with **`gemini-assist/`**) — or the `FIX_UNKNOWN` line above when `[CVD_HEALTHY]` with zero warnings. Follow your skill instructions.
