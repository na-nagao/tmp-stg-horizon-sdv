Run root cause analysis for CVD Launcher / Cuttlefish failures only. Step 1 output is in step1_output.md (context may be appended below). **Honour the `CVD_STATUS` header** from Phase 0: on `BOOT_OK`, focus RCA on **runtime** domains (logcat crashes, late kernel faults, WiFi/DHCP, resource pressure) and do not frame findings as boot failures; on `[CVD_HEALTHY]` emit a single confirmation section — do not invent findings. On `BOOT_FAILED` / `BOOT_UNKNOWN`, keep the existing guest-kernel-first priority. Follow your skill instructions.
