Triage Cuttlefish/CVD artifacts under test-results (workspace-relative; not host path /test-results). **Userspace logcat** is usually a file named **`logcat`** under **`**/logs/`** (not `kernel.log`). **Output:** start with **`## CVD boot preflight`** (Phase 0: `CVD_STATUS` BOOT_OK|BOOT_FAILED|BOOT_UNKNOWN, `GUESTS_EXPECTED`, `GUESTS_RUNNING`, `EVIDENCE`), then **`## Kernel preflight (machine-readable)`** and **`## Logcat preflight (machine-readable)`** (see skill) before the Phase 3 table; optional **`## Logcat excerpts (verbatim)`**. If **`test-results/cuttlefish_logs*.zip`** exists and no guest **`kernel.log`** under **`test-results/cvd/`** yet, **`unzip`** into **`test-results/cvd/`** first (mandatory per skill). Do not triage from flattened **`test-results/cvd-*.log`** alone — guest **`test-results/cvd/**/logs/kernel.log`** first (Phase 1b-A / 1b), **`launcher.log`**, then **Phase 1d** on all logcat-like paths (**exception-first** combined native+Java sweep, then conditional ANR/framework/DEBUG — **`triage-cvd` Phase 1d**), then WiFi. Emit **`[GUEST_KERNEL_*]`** and **`[LOGCAT_*]`** when greps match. **Lane routing:** when `CVD_STATUS=BOOT_OK`, Phase 1b-A bootconfig warnings are **informational** only (no mandatory `[GUEST_BOOT_FAILED]` row) and Phase 1d/runtime findings are primary; if nothing actionable, emit one Phase 3 row `[NO_RUNTIME_ISSUE]` bucket `[CVD_HEALTHY]`. When `CVD_STATUS=BOOT_FAILED` or `BOOT_UNKNOWN`, treat **`Failed to parse bootconfig`** / **`Value is redefined`** as mandatory **[GUEST_BOOT_FAILED]** rows when applicable. Unrelated files under test-results: note briefly and continue with CVD logs. Follow your skill instructions.
