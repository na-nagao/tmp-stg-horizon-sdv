# Copyright (c) 2024-2026 Accenture, All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
# WorkflowTemplates for module sample-hard (not exposed on Horizon API catalog). Argo Events: templates/sensors.yaml.
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: sample-hard-smoke-test
  namespace: {{ .Values.workflowNamespace }}
  labels:
    app.kubernetes.io/name: smoke-test
    horizon-sdv.io/module: sample-hard
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  entrypoint: run-smoke-test
  serviceAccountName: workflow-executor
  arguments:
    parameters:
      - name: horizonSubmittedFrom
        value: ""
      - name: sampleEnv
        value: ""
      - name: sampleBuildId
        value: ""
      - name: sampleNote
        value: ""
  templates:
    - name: run-smoke-test
      dag:
        tasks:
          - name: log-parameters
            template: log-parameters
          - name: echo-and-artifact
            template: echo-and-artifact
            dependencies: [log-parameters]
          - name: verify-gcs
            template: verify-gcs
            dependencies: [echo-and-artifact]
    - name: log-parameters
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "WorkflowTemplate: smoke-test"
            echo "sampleEnv={{ "{{workflow.parameters.sampleEnv}}" }}"
            echo "sampleBuildId={{ "{{workflow.parameters.sampleBuildId}}" }}"
            echo "sampleNote={{ "{{workflow.parameters.sampleNote}}" }}"
            echo "at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
    - name: echo-and-artifact
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "Smoke test running at $(date)"
            echo "SA: $(cat /var/run/secrets/kubernetes.io/serviceaccount/namespace)/$(cat /var/run/secrets/kubernetes.io/serviceaccount/..data/namespace 2>/dev/null || echo 'n/a')"
            echo "Smoke test passed - $(date -u +%Y-%m-%dT%H:%M:%SZ)" > /tmp/result.txt
            cat /tmp/result.txt
      outputs:
        artifacts:
          - name: smoke-result
            path: /tmp/result.txt
    - name: verify-gcs
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "Artifact upload verified by reaching this step."
            echo "All checks passed."
---
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: sample-hard-smoke-test-alt
  namespace: {{ .Values.workflowNamespace }}
  labels:
    app.kubernetes.io/name: smoke-test
    horizon-sdv.io/module: sample-hard
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  entrypoint: run-smoke-test
  serviceAccountName: workflow-executor
  arguments:
    parameters:
      - name: horizonSubmittedFrom
        value: ""
      - name: releaseChannel
        value: ""
      - name: ticketId
        value: ""
      - name: owner
        value: ""
  templates:
    - name: run-smoke-test
      dag:
        tasks:
          - name: log-parameters
            template: log-parameters
          - name: echo-and-artifact
            template: echo-and-artifact
            dependencies: [log-parameters]
          - name: verify-gcs
            template: verify-gcs
            dependencies: [echo-and-artifact]
    - name: log-parameters
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "WorkflowTemplate: smoke-test-alt (variant)"
            echo "releaseChannel={{ "{{workflow.parameters.releaseChannel}}" }}"
            echo "ticketId={{ "{{workflow.parameters.ticketId}}" }}"
            echo "owner={{ "{{workflow.parameters.owner}}" }}"
            echo "at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
    - name: echo-and-artifact
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "Smoke test running at $(date)"
            echo "SA: $(cat /var/run/secrets/kubernetes.io/serviceaccount/namespace)/$(cat /var/run/secrets/kubernetes.io/serviceaccount/..data/namespace 2>/dev/null || echo 'n/a')"
            echo "Smoke test passed - $(date -u +%Y-%m-%dT%H:%M:%SZ)" > /tmp/result.txt
            cat /tmp/result.txt
      outputs:
        artifacts:
          - name: smoke-result
            path: /tmp/result.txt
    - name: verify-gcs
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - |
            echo "Artifact upload verified by reaching this step."
            echo "All checks passed."
