# Copyright (c) 2026 Accenture, All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: mcp-gateway-route
  namespace: mcp-gateway-registry
  labels:
    gateway: gke-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  parentRefs:
    - kind: Gateway
      name: gke-gateway
      namespace: gke-gateway
      sectionName: https

    ## add by n.nagao

    # 内部向け（正解）：Internal Gateway の HTTP リスナー
    - kind: Gateway
      name: sdv-internal-gateway
      namespace: gke-gateway     # ✅ 修正点
      sectionName: https          # ✅ 追加
    ##########################


  hostnames:
    - {{ printf "mcp.%s" .Values.config.domain | quote }}
    - mcp.cdc.horizon-sdv-stg
  rules:
    # route `mcp.domain` to nginx port inside registry service
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: registry
          namespace: mcp-gateway-registry
          port: 80
    # route `/auth-server` to auth-server service; strip prefix `/auth-server` to `/`
    - matches:
        - path:
            type: PathPrefix
            value: /auth-server
      filters:
        - type: URLRewrite
          urlRewrite:
            #hostname: {{ printf "mcp.%s" .Values.config.domain | quote }} #change by nagao
            hostname: mcp.cdc.horizon-sdv-stg
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: auth-server
          namespace: mcp-gateway-registry
          port: 8888
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: mcp-gateway-registry-healthcheck
  namespace: mcp-gateway-registry
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  default:
    checkIntervalSec: 15
    timeoutSec: 15
    healthyThreshold: 1
    unhealthyThreshold: 2
    logConfig:
      enabled: true
    config:
      type: HTTP
      httpHealthCheck:
        port: 7860
        requestPath: /health
  targetRef:
    group: ""
    kind: Service
    name: registry
    namespace: mcp-gateway-registry
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: mcp-gateway-auth-server-healthcheck
  namespace: mcp-gateway-registry
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  default:
    checkIntervalSec: 15
    timeoutSec: 15
    healthyThreshold: 1
    unhealthyThreshold: 2
    logConfig:
      enabled: true
    config:
      type: HTTP
      httpHealthCheck:
        port: 8888
        requestPath: /health
  targetRef:
    group: ""
    kind: Service
    name: auth-server
    namespace: mcp-gateway-registry
